describe('Anderes Ding', function() {
	it('should open on startup', function() {
	});
	it('should be closed after click in open range', function() {
	});
	
	
	describe('Filter', function() {
		it('should be visible', function() {
			expect(5).toBe(5);
		});
		
		describe('Meins', function() {
			it('should only be visible for users with right', function() {
			});
			it('should contain something', function() {
				expect(5).toBe(2);
			});
		});
		describe('All', function() {
			it('should only be visible for users with right', function() {
			});
			it('should contain all patients', function() {
			});
		});
	});
	
	describe('Statistics', function(){
		it('should be possible to see number of patients', function(){
			
		});
		it('should be possible to see number of shown patients', function(){
			
		});
		it('should be possible to see number of hidden patients', function(){
			expect( true ).toBe( false );
		});
		
	});
});
